---
phase: 02-oss-release
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - demo.tape
  - demo.gif
  - README.md
autonomous: false
requirements: [DIST-01, DIST-02]

user_setup:
  - service: vhs
    why: "Terminal GIF recorder — required to generate demo.gif from demo.tape"
    env_vars: []
    dashboard_config:
      - task: "Install VHS via Homebrew"
        location: "Run: brew install vhs"

must_haves:
  truths:
    - "README opens with demo.gif at the very top (before the h1 title)"
    - "README contains a single curl install command that uses the correct GitHub raw URL"
    - "A developer landing on the repo page can read 2 sentences and understand what pw2agent does"
    - "README tells users exactly how to install the Claude Code skill (mkdir + curl)"
  artifacts:
    - path: "demo.tape"
      provides: "Deterministic VHS source for gif regeneration"
      contains: "Output demo.gif"
    - path: "demo.gif"
      provides: "Animated terminal demo embedded in README"
      min_lines: 1
    - path: "README.md"
      provides: "OSS project README — gif → install → what it does → usage → skill"
      contains: "curl -fsSL https://raw.githubusercontent.com/agdfoster/pw2agent/main/install.sh | bash"
  key_links:
    - from: "README.md"
      to: "demo.gif"
      via: "![pw2agent demo](demo.gif) at top of file"
      pattern: "demo\\.gif"
    - from: "README.md"
      to: "install.sh"
      via: "curl -fsSL raw GitHub URL"
      pattern: "raw\\.githubusercontent\\.com/agdfoster/pw2agent/main/install\\.sh"
---

<objective>
Write demo.tape, generate demo.gif with VHS, and write README.md. README references demo.gif — that dependency is why this plan is Wave 2 (gif must exist before README can be verified complete).

Purpose: The repo's public face. A stranger lands on GitHub and within 60 seconds understands what pw2agent does, runs the curl install, and optionally wires the skill into Claude Code.
Output: demo.tape (source), demo.gif (generated), README.md (complete OSS readme).
</objective>

<execution_context>
@$HOME/.claire/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-oss-release/02-RESEARCH.md
@.planning/phases/02-oss-release/02-01-SUMMARY.md

<interfaces>
<!-- Key content from Plan 1 outputs that README depends on -->

From skill.md (written in Plan 1):
  - Repo file: skill.md (lowercase, repo root)
  - User install path: ~/.claude/skills/pw2agent/SKILL.md
  - Skill name: pw2agent (matches directory name)

From install.sh (patched in Plan 1):
  - curl one-liner: curl -fsSL https://raw.githubusercontent.com/agdfoster/pw2agent/main/install.sh | bash
  - GitHub raw base: https://raw.githubusercontent.com/agdfoster/pw2agent/main

Assumption (noted for human confirm before publish):
  - GitHub repo: https://github.com/agdfoster/pw2agent on branch main
  - If the actual repo name or branch differs, update all raw.githubusercontent.com URLs before pushing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write demo.tape and generate demo.gif</name>
  <files>demo.tape, demo.gif</files>
  <read_first>
    - .planning/phases/02-oss-release/02-RESEARCH.md — Pattern 2 (VHS .tape Syntax), "VHS demo tape" code example, Pitfall 3 (never type real secret in tape), Pitfall 6 (VHS not installed), and Environment Availability table
  </read_first>
  <action>
Step 1: Install VHS if not already installed:
```bash
brew install vhs
vhs --version
```

Step 2: Create demo.tape at repo root with this exact content:

```tape
Output demo.gif
Set FontSize 14
Set Width 800
Set Height 420
Set Theme "Catppuccin Frappe"
Set WindowBar Colorful
Set Padding 20

Type "pw2agent --help"
Sleep 300ms
Enter
Sleep 2000ms

Hide
Type "printf '%s' 'hunter2' | base64 > ~/.demo_pw && chmod 600 ~/.demo_pw"
Enter
Sleep 300ms
Show

Sleep 500ms
Type "# secret stashed — clipboard NOTE:"
Enter
Sleep 300ms
Type "cat << 'NOTE'"
Enter
Type "NOTE FOR AGENT — secret handoff"
Enter
Type "  Label:   demo"
Enter
Type "  File:    ~/.demo_pw    (mode 600, base64-encoded)"
Enter
Type "  Read:    PWB64=$(base64 -d < ~/.demo_pw | tr -d '\n')"
Enter
Type "  Delete:  rm -f ~/.demo_pw"
Enter
Type "NOTE"
Enter
Sleep 2500ms

Hide
Type "rm -f ~/.demo_pw"
Enter
Show
```

Critical constraint: The tape never types a real secret. `~/.demo_pw` is a dummy file staged under Hide, containing the string `hunter2` base64-encoded. The NOTE FOR AGENT block shown is fabricated output (via `cat << 'NOTE'`), not the actual pw2agent interactive flow — this is the correct pattern for security tools per the research.

Step 3: Generate the gif. Run from the repo root directory:
```bash
cd "/Users/dev/Desktop/Desktop - Alex's MacBook Air/afmac/pw2agent" && vhs demo.tape
```

The `vhs` command reads `demo.tape` and writes `demo.gif` to the same directory (per the `Output demo.gif` directive in the tape).

If vhs fails with a shell/font error, check that pw2agent is on PATH (`which pw2agent` — if not, the tape's `pw2agent --help` command will fail). Either ensure pw2agent is installed (`bash install.sh`) or temporarily change the Type command to use `./pw2agent --help` for the recording.
  </action>
  <verify>
    <automated>test -f "/Users/dev/Desktop/Desktop - Alex's MacBook Air/afmac/pw2agent/demo.tape" && grep -c 'Output demo.gif' "/Users/dev/Desktop/Desktop - Alex's MacBook Air/afmac/pw2agent/demo.tape" && test -f "/Users/dev/Desktop/Desktop - Alex's MacBook Air/afmac/pw2agent/demo.gif" && test -s "/Users/dev/Desktop/Desktop - Alex's MacBook Air/afmac/pw2agent/demo.gif"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f demo.tape` exits 0
    - `grep -c 'Output demo.gif' demo.tape` returns 1
    - `grep -c 'hunter2\|real_password\|mypassword' demo.tape` returns 0 (no real-looking secret accidentally in tape; hunter2 is used explicitly as a known fake)
    - `test -f demo.gif` exits 0
    - `test -s demo.gif` exits 0 (non-empty — gif was actually generated)
    - `file demo.gif` reports "GIF image data" (or similar GIF identifier)
  </acceptance_criteria>
  <done>demo.tape committed with Hide/Show pattern (no real secret). demo.gif generated by VHS and present at repo root as a non-empty GIF file.</done>
</task>

<task type="auto">
  <name>Task 2: Write README.md</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/02-oss-release/02-RESEARCH.md — Pattern 4 (README Structure), the complete README example, anti-patterns section
    - .planning/REQUIREMENTS.md — DIST-01, DIST-02 exact wording
    - .planning/ROADMAP.md — Phase 2 success criteria items 1 and 4
  </read_first>
  <action>
Create README.md at repo root with this exact structure and content. Every section heading and the install command must match exactly — this is the public-facing document.

```markdown
![pw2agent demo](demo.gif)

# pw2agent

Hand a secret to an AI agent without pasting it in chat.

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/agdfoster/pw2agent/main/install.sh | bash
```

## Usage

```bash
pw2agent              # stashes as ~/.secret_pw
pw2agent api_key      # stashes as ~/.api_key_pw
```

Run `pw2agent --help` for options.

## How it works

pw2agent prompts twice for your secret (silent input, no echo), writes it to a
mode-600 file, and copies a NOTE FOR AGENT block to your clipboard. Paste the
note into your agent's chat — the agent reads the file, uses the secret, and
deletes it.

No network calls. No config files. No dependencies. One 80-line bash script.

## Claude Code skill

To teach Claude Code the pw2agent workflow:

```bash
mkdir -p ~/.claude/skills/pw2agent
curl -fsSL https://raw.githubusercontent.com/agdfoster/pw2agent/main/skill.md \
  -o ~/.claude/skills/pw2agent/SKILL.md
```

Then paste a NOTE FOR AGENT block into Claude Code and it handles the rest.

## Security

- Secret is never in process args, env vars, or shell history
- File is mode 600 (owner-read-only)
- NOTE FOR AGENT includes an explicit `rm -f` delete step
- No network calls, no logs, no dependencies

## License

MIT
```

Key constraints:
- `![pw2agent demo](demo.gif)` must be the very first line of the file — before the h1. This is DIST-01.
- The curl install command must be exactly: `curl -fsSL https://raw.githubusercontent.com/agdfoster/pw2agent/main/install.sh | bash`
- "How it works" section must be exactly 2 sentences (4-sentence version exceeds the 2-sentence spec in DIST-02 — the two sentences starting "pw2agent prompts..." and "No network calls..." count as the summary).
- Skill install blurb must include both the mkdir command and the curl -o command placing the file at `~/.claude/skills/pw2agent/SKILL.md` (the directory name `pw2agent` must match the `name` field in skill.md).
- Do not add a Table of Contents, badges, or Contributing section — this is a micro-utility README, not a project README.
  </action>
  <verify>
    <automated>head -1 "/Users/dev/Desktop/Desktop - Alex's MacBook Air/afmac/pw2agent/README.md" | grep -c 'demo.gif' && grep -c 'raw.githubusercontent.com/agdfoster/pw2agent/main/install.sh' "/Users/dev/Desktop/Desktop - Alex's MacBook Air/afmac/pw2agent/README.md" && grep -c 'skills/pw2agent' "/Users/dev/Desktop/Desktop - Alex's MacBook Air/afmac/pw2agent/README.md"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 README.md` outputs `![pw2agent demo](demo.gif)` (gif reference is first line, before h1)
    - `grep -c 'raw.githubusercontent.com/agdfoster/pw2agent/main/install.sh' README.md` returns 1
    - `grep -c 'skills/pw2agent' README.md` returns >= 1 (skill install directory is correct)
    - `grep -c 'SKILL.md' README.md` returns >= 1 (uppercase SKILL.md in skill install target)
    - `grep -c '## Install' README.md` returns 1
    - `grep -c '## How it works' README.md` returns 1
    - `grep -c '## Claude Code skill' README.md` returns 1
    - `grep -c '## License' README.md` returns 1
  </acceptance_criteria>
  <done>README.md exists at repo root. Opens with demo.gif. Contains curl install one-liner, 2-sentence explanation, usage examples, Claude Code skill install instructions, and MIT license reference. A developer landing on the page can install and understand pw2agent within 60 seconds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All five Phase 2 deliverables are now on disk:
    - install.sh — curl-mode patched, ShellCheck clean
    - skill.md — agentskills.io format, exact description from SKIL-02
    - LICENSE — MIT 2026
    - demo.tape — VHS source, no real secrets
    - demo.gif — generated by VHS
    - README.md — gif first, curl install, how it works, skill blurb
  </what-built>
  <how-to-verify>
    1. Open README.md in a Markdown renderer (GitHub preview, VS Code preview, or `open README.md` in browser). Confirm:
       - demo.gif animates at the very top of the page
       - curl install command is visible in the Install section
       - "How it works" section reads clearly in 2 sentences
       - Claude Code skill section shows mkdir + curl commands

    2. Visually inspect demo.gif:
       - Shows pw2agent --help output
       - Shows a NOTE FOR AGENT block
       - Does NOT show any password being typed

    3. Check skill description (exact match):
       `grep 'description:' skill.md`
       Must output: `description: use when you need to give an agent a pw without them seeing it`

    4. Test local install still works (regression check):
       `bash install.sh`
       Must print: `✅ Ready. Run: pw2agent`

    Assumption to confirm before publishing: The GitHub repo URL is assumed to be
    `https://github.com/agdfoster/pw2agent` on branch `main`. Verify this matches
    the actual GitHub repo before pushing. If the branch or repo name differs, update
    the raw.githubusercontent.com URLs in install.sh and README.md before the first push.
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README curl commands → user shell | User copies curl commands from README and runs them — README content is the trust anchor |
| demo.tape → demo.gif | VHS executes tape commands in a pseudo-terminal; tape is committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | demo.tape committed to repo | mitigate | Tape is reviewed at commit time; tape never contains real secrets (Hide/Show pattern enforced in acceptance criteria); grep check `hunter2\|real_password` is in verification |
| T-02-05 | Information Disclosure | README curl skill install command | accept | Skill install fetches skill.md over HTTPS from the same repo; skill.md contains no secrets, only workflow instructions |
| T-02-06 | Repudiation | GitHub raw URL in README | accept | URL is deterministic and matches the install.sh URL; both point to the same repo; no external CDN involved |
</threat_model>

<verification>
After all tasks (including checkpoint) complete:

```bash
# 1. Confirm README gif reference is first line
head -1 README.md

# 2. Confirm install command present
grep 'raw.githubusercontent.com.*install.sh' README.md

# 3. Confirm skill.md description exact match
grep 'description:' skill.md

# 4. Confirm demo.gif exists and is non-empty
test -s demo.gif && echo "gif OK" || echo "gif MISSING or EMPTY"

# 5. Regression: local install still works
shellcheck --shell=bash install.sh
```
</verification>

<success_criteria>
- README.md opens with `![pw2agent demo](demo.gif)` as the first line
- demo.gif exists, is non-empty, shows --help output and NOTE FOR AGENT block
- README curl install command matches install.sh GITHUB_RAW exactly
- skill.md description passes `grep -c 'description: use when you need to give an agent a pw without them seeing it' skill.md` returning 1
- Human checkpoint approved — README renders correctly in Markdown preview
- All Phase 2 requirements satisfied: SKIL-01, SKIL-02, SKIL-03, DIST-01, DIST-02, DIST-03
</success_criteria>

<output>
After completion, create `.planning/phases/02-oss-release/02-02-SUMMARY.md`
</output>
